# Screenshots

Add the screenshots described in `portfolio/README.md` to this directory.

Recommendations:

- use the same resolution and demo project throughout;
- keep the interface readable without relying on browser zoom;
- use fictional data;
- avoid crops that hide the context of the feature.

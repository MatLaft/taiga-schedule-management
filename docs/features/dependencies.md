# Dependencies

Activities can be connected visually in the Gantt chart. Links are persisted
by the backend and can trigger cascading date changes across related
activities.

Creating and removing links requires `modify_schedule_links`. Resulting date
changes require `modify_schedule_dates`.

A visual example will be added later at
`portfolio/screenshots/dependencies.png`.
